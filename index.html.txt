<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>JROTC Forms Manager</title>
  <link rel="stylesheet" href="styles.css" />
  <!-- SheetJS (xlsx) for Excel import/export -->
  <script src="https://cdn.jsdelivr.net/npm/xlsx@0.18.5/dist/xlsx.full.min.js"></script>
</head>
<body>
  <div id="login-screen" class="overlay">
    <div class="login-box">
      <h2>Sign In</h2>
      <div class="field">
        <label>Username</label>
        <input id="login-username" autocomplete="username" />
      </div>
      <div class="field">
        <label>Password</label>
        <input id="login-password" type="password" autocomplete="current-password" />
      </div>
      <div class="actions">
        <button id="login-btn">Log In</button>
        <div id="login-error" class="error"></div>
      </div>
      <small>Username and password are case-sensitive.</small>
    </div>
  </div>

  <header>
    <h1>JROTC Forms Manager</h1>
    <div class="header-actions">
      <div class="user-info" id="user-info" style="display:none">
        <span id="current-user"></span>
        <button id="logout-btn" title="Log out" class="small secondary">Log out</button>
      </div>
      <input id="search" placeholder="Search names..." />
      <button id="import-btn">Import Excel</button>
      <input type="file" id="file-input" accept=".xlsx,.xls,.csv" style="display:none" />
      <button id="add-btn">Add Name</button>
      <button id="export-csv-btn">Export CSV</button>
      <button id="export-xlsx-btn">Export Excel</button>
      <button id="clear-btn" title="Clear all stored data">Clear All Data</button>
    </div>
  </header>

  <main>
    <section id="add-row" class="hidden">
      <input id="new-name" placeholder="Full name" />
      <button id="save-new">Save</button>
      <button id="cancel-new">Cancel</button>
    </section>

    <table id="names-table">
      <thead>
        <tr>
          <th>Name</th>
          <th>Onboarding</th>
          <th>3203</th>
          <th>Medical Release</th>
          <th>Risk Screening</th>
          <th>Physical</th>
          <th>Actions</th>
        </tr>
      </thead>
      <tbody>
        <!-- populated by JS -->
      </tbody>
    </table>

    <div id="empty-msg" class="empty">No names yet. Import from Excel or add a name.</div>
  </main>

  <script src="app.js"></script>
</body>
</html>